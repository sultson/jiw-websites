export type Lang = 'nl' | 'en';

export const translations: Record<Lang, Record<string, string>> = {
  nl: {
    // Vul hier Nederlandse vertalingen in
    // Voorbeeld: 'nav.home': 'Home',
  },
  en: {
    // Vul hier Engelse vertalingen in
    // Voorbeeld: 'nav.home': 'Home',
  },
};
