/* GENERATED by scripts/i18n-strings.mjs. Do not hand-edit.
   pt/index.ts is a 1:1 structural mirror of en/index.ts; only leaf strings
   differ. To change structure, change English and re-run the build. */
import type { SiteContent } from '../types';
import { core } from './core';
import { immigration } from './immigration';
import { housing } from './housing';
import { relocation } from './relocation';
import { industrial } from './industrial';
import { business } from './business';
import { vip } from './vip';
import { guides } from './guides';

export const pt: SiteContent = {
  core,
  immigration,
  housing,
  relocation,
  industrial,
  business,
  vip,
  guides,
};
