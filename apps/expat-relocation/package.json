{
  "name": "@jiw/expat-relocation",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev --port 3019 --host",
    "build": "astro build",
    "audit:migration": "node scripts/audit-migration-redirects.mjs",
    "audit:seo": "node scripts/audit-search-metadata.mjs",
    "preview": "astro preview",
    "preview:pages": "wrangler pages dev dist",
    "ship": "pnpm build && pnpm audit:seo && pnpm audit:migration && wrangler pages deploy dist --project-name expat-relocation --branch main",
    "ship:dry-run": "pnpm build && pnpm audit:seo && pnpm audit:migration && wrangler pages functions build functions --outdir .wrangler/pages-functions",
    "ship:forms": "wrangler deploy --config wrangler.forms.jsonc",
    "ship:forms:dry-run": "wrangler deploy --config wrangler.forms.jsonc --dry-run",
    "clean": "rm -rf dist .astro",
    "lint": "astro check && tsc --noEmit -p tsconfig.worker.json",
    "optimize-images": "node scripts/optimize-images.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "@fontsource-variable/montserrat": "^5.2.8",
    "@fontsource-variable/playfair-display": "^5.2.8",
    "@jiw/cloudflare-forms": "workspace:*",
    "@tailwindcss/vite": "^4.1.14",
    "astro": "^5.16.0",
    "tailwindcss": "^4.1.14"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@cloudflare/workers-types": "^4.20260424.1",
    "sharp": "^0.34.5",
    "typescript": "~5.8.2",
    "vite": "^6.4.2",
    "wrangler": "^4.85.0"
  }
}
